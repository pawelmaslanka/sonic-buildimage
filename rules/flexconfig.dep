
SPATH       := $($(FLEXCONFIG)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/flexconfig.mk rules/flexconfig.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(FLEXCONFIG)_CACHE_MODE  := GIT_CONTENT_SHA 
$(FLEXCONFIG)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(FLEXCONFIG)_DEP_FILES   := $(DEP_FILES)

