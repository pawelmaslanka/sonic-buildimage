
SPATH       := $($(NET_MIDDLEWARE)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/net-middleware.mk rules/net-middleware.dep   
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(SPATH))

$(NET_MIDDLEWARE)_CACHE_MODE  := GIT_CONTENT_SHA 
$(NET_MIDDLEWARE)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(NET_MIDDLEWARE)_DEP_FILES   := $(DEP_FILES)
